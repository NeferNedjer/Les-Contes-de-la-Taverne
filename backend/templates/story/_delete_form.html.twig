<form method="post" action="{{ path('app_story_delete', {'id': story.id}) }}" onsubmit="return confirm('Vous êtes sûre de vouloir supprimer cette histoire ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ story.id) }}">
    <button class="btn">Supprimer</button>
</form>
