{% extends 'base.html.twig' %}

{% block title %}Modifier une histoire{% endblock %}

{% block body %}
<section class="card" style="margin-bottom: 16px;">
    <h1>Modifier une histoire</h1>
    {{ include('story/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
    {{ include('story/_delete_form.html.twig') }}
    <p style="display:flex; justify-content:flex-end; gap:8px; margin-bottom: 12px;">
    <a class="btn" href="{{ path('app_home') }}">Retour à l'accueil</a>
    <a class="btn" href="{{ path('app_story_index') }}">Retour à la liste</a>
    </p>
</section>

    
{% endblock %}
